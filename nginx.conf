worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Cloudflare IP диапазони (IPv4 & IPv6)
    # https://www.cloudflare.com/ips/
    geo $realip_remote_addr $cloudflare_ip {
        default 0;

        # IPv4
        103.21.244.0/22 1;
        103.22.200.0/22 1;
        103.31.4.0/22 1;
        104.16.0.0/13 1;
        104.24.0.0/14 1;
        108.162.192.0/18 1;
        131.0.72.0/22 1;
        141.101.64.0/18 1;
        162.158.0.0/15 1;
        172.64.0.0/13 1;
        173.245.48.0/20 1;
        188.114.96.0/20 1;
        190.93.240.0/20 1;
        197.234.240.0/22 1;
        198.41.128.0/17 1;

        # IPv6
        2400:cb00::/32 1;
        2405:8100::/32 1;
        2405:b500::/32 1;
        2606:4700::/32 1;
        2803:f800::/32 1;
        2c0f:f248::/32 1;
        2a06:98c0::/29 1;
    }

    # Забраняване на директни връзки (само през Cloudflare)
    map $cloudflare_ip $allow_cloudflare {
        0 "403";
        1 "";
    }

    limit_req_log_level notice;

    # Лимитиране на базата на реалния IP (от Cloudflare хедър)
    limit_req_zone $realip_remote_addr zone=auth_limit:10m rate=10r/m;
    limit_req_zone $realip_remote_addr zone=game_limit:10m rate=30r/m;
    limit_req_zone $realip_remote_addr zone=user_limit:10m rate=10r/m;
    limit_conn_zone $realip_remote_addr zone=addr_limit:10m;
    limit_req_status 429;

    # 1. HTTP -> HTTPS Redirect
    server {
        listen 80;
        server_name deck.bg www.deck.bg;

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # 2. Основен HTTPS Сървър
    server {
        listen 443 ssl;
        server_name deck.bg www.deck.bg;

        # Забраняване на директни връзки
        if ($allow_cloudflare = "403") {
            return 403 "Access denied. Please use Cloudflare.";
        }

#         # Пътища до сертификатите
#         ssl_certificate /etc/letsencrypt/live/deck.bg/fullchain.pem;
#         ssl_certificate_key /etc/letsencrypt/live/deck.bg/privkey.pem;

        # NEW Cloudflare certificates:
        ssl_certificate /etc/ssl/cloudflare/origin.pem;
        ssl_certificate_key /etc/ssl/cloudflare/private.key;

        # Сигурност за SSL
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # Настройки за Cloudflare Real IP
        real_ip_header CF-Connecting-IP;
        real_ip_recursive on;

        # Cloudflare trusted proxies
        set_real_ip_from 103.21.244.0/22;
        set_real_ip_from 103.22.200.0/22;
        set_real_ip_from 103.31.4.0/22;
        set_real_ip_from 104.16.0.0/13;
        set_real_ip_from 104.24.0.0/14;
        set_real_ip_from 108.162.192.0/18;
        set_real_ip_from 131.0.72.0/22;
        set_real_ip_from 141.101.64.0/18;
        set_real_ip_from 162.158.0.0/15;
        set_real_ip_from 172.64.0.0/13;
        set_real_ip_from 173.245.48.0/20;
        set_real_ip_from 188.114.96.0/20;
        set_real_ip_from 190.93.240.0/20;
        set_real_ip_from 197.234.240.0/22;
        set_real_ip_from 198.41.128.0/17;
        set_real_ip_from 2400:cb00::/32;
        set_real_ip_from 2405:8100::/32;
        set_real_ip_from 2405:b500::/32;
        set_real_ip_from 2606:4700::/32;
        set_real_ip_from 2803:f800::/32;
        set_real_ip_from 2c0f:f248::/32;
        set_real_ip_from 2a06:98c0::/29;

        # Глобални хедъри за проксиране
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $realip_remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header CF-Connecting-IP $realip_remote_addr;

        # Frontend
        location / {
            proxy_pass http://frontend:80;
        }

        # Auth API с лимит 10r/m
        location /api/auth/ {
            limit_req zone=auth_limit burst=5 nodelay;
            proxy_pass http://backend:8080/auth/;
        }

        # Game API
        location /api/game/ {
            limit_req zone=game_limit burst=10 nodelay;
            proxy_pass http://backend:8080/game/;
            proxy_set_header Authorization $http_authorization;
        }

        # User API
        location /api/user/ {
            limit_req zone=user_limit burst=5 nodelay;
            proxy_pass http://backend:8080/user/;
            proxy_set_header Authorization $http_authorization;
        }

        # WebSocket
        location /api/ws-game/ {
            proxy_pass http://backend:8080/ws-game/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
        }
    }
}